/**
 * Contains all database access to the tag database. There are two databases maintained by this package.
 * The History table stores a history of all used tags (see HistoryDb). The TagDb contains information on all tags.
 */
package tracebook.core.data.db;